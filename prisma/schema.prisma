generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model events {
  id          Int      @id @default(autoincrement())
  title       String
  description String?
  event_date  DateTime @db.Date
}
